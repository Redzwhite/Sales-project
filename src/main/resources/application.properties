spring.datasource.url = ${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username = ${DB_USER}
spring.datasource.password = ${DB_PASSWORD}
server.port=${APP_PORT}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.liquibase.change-log=classpath:/db/changelog/master.xml
spring.liquibase.url=${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.liquibase.user=${DB_USER}
spring.liquibase.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


##Flyway properties
#spring.flyway.enabled=true
#spring.flyexport LIQUIBASE_URL="jdbc:mysql://localhost:3306/dating_admin_tracker"way.url=${DB_HOST}:${DB_PORT}/${DB_NAME}
#spring.flyway.password= ${DB_PASSWORD}
#spring.flyway.user= ${DB_USER}
#spring.flyway.schemas=dating_admin_tracker
#spring.flyway.locations=classpath:db/migration/dating_admin_tracker
#spring.flyway.baseline-on-migrate=true
#logging.level.org.springframework.security=DEBUG